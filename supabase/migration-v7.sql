-- Swift Portal V7 — status workflow cleanup (Part 1: run first, then v7b)
-- Maps legacy status values; no new enum values required for most deployments.

-- Optional: if you previously added 'approved' as separate step, data migration in v7b handles it.
